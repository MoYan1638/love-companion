# -*- coding: utf-8 -*-
"""
趋势感知（M7a / 阶段8）

模块划分（「源路由 + 知识库」两段式）：
- sources.py  平台注册表与故障转移：产出 primary + fallback 抓取链，
              记录各源成败，挂了的自动排到后面。**不发网络请求**
- store.py    趋势知识库：原文离线提炼成短要点、按主题归档、在线只注入
              一句「趋势调味料」（默认 ≤50 Token）

状态：M7a 已实现（2026-09）。

设计约束（红线）：
- 只依赖 Python 标准库，零外部依赖（Skill 分发的硬性要求）
- 原始抓取内容**绝不进上下文**，只留提炼后的要点
- 主题取自用户画像「互动偏好.喜欢的话题」，不主观臆测用户兴趣
- 单轮额外注入预算见 core/schema.py（趋势调味料 50）
"""
