# -*- coding: utf-8 -*-
"""
主动关怀与情感支持（M6 / 阶段6）

模块划分：
- trigger.py   触发判定：信号识别（深夜/低落/纪念日/久未联系）+ 三道闸门（冷却 / 静默 / 频次上限）
- templates.py 话术生成：模板 + 声线改写 + 纠偏层 + 预算截断

状态：M6 已实现（2026-09）。

设计约束（红线）：
- 只依赖 Python 标准库，零外部依赖（Skill 分发的硬性要求）
- 重活全部离线完成，在线只注入摘要
- 用户「主动程度偏好=低」时一律不打扰，数据主权优先于存在感
- 话术必须能落到具体事实，填不出细节就换更短的模板，绝不用万能句
- 单轮额外注入预算见 core/schema.py（关怀话术 80）

⚠️ 已知外部约束：是否真能「主动开口」取决于 IM 通道支持与否。
通道不支持时退化为「下次对话开头带一句」，话术本身照常生成。
"""
